# API base URL — in dev, Vite proxies /v1 to localhost:3000 (leave blank)
VITE_API_URL=

# Tenant UUID for local development
# Get this from the seed output or:
#   docker exec leadops_postgres psql -U leadops leadops_dev -c "SELECT id FROM tenants WHERE slug='local';"
VITE_TENANT_ID=
